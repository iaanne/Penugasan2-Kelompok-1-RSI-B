services:
  db:
    image: postgres:16
    container_name: rsi_postgres
    environment:
      POSTGRES_USER: archian
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: rsi_db
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:

